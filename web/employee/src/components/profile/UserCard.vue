<template>
    <el-card style="margin-bottom:20px;">
        <div slot="header" class="clearfix">
            <span>About me</span>
        </div>

        <div class="user-profile">
            <div class="box-center">
                <pan-thumb :height="'100px'" :width="'100px'" :hoverable="false">
                    <div>Hello</div>
                   {{user.username}}
                </pan-thumb>
            </div>
            <div class="box-center">
                <div class="user-name text-center"></div>
                <div class="user-role text-center text-muted"></div>
            </div>
        </div>

        <div class="user-bio">
            <div class="user-education user-bio-section">
                <div class="user-bio-section-header">
                    <el-icon class="el-icon-reading"/>
                    <span>Education</span></div>
                <div class="user-bio-section-body">
                    <div class="text-muted">
                        JS in Computer Science from the University of Technology
                    </div>
                </div>
            </div>

            <div v-if="skills.length > 0" class="user-skills user-bio-section">
                <div class="user-bio-section-header">
                    <el-icon class="el-icon-cpu"/>
                    <span>Skills</span></div>
                <div class="user-bio-section-body">
                    <div class="progress-item" v-for="(item,i) in skills" :key="i">
                        <span>{{item.name}}</span>
                        <el-progress :percentage="item.percentage"></el-progress>
                    </div>
                </div>
            </div>
        </div>
    </el-card>
</template>

<script>
    import PanThumb from '../PanThumb/index'

    export default {
        components: {PanThumb},
        props: {
            user: {
                type: Object,
                default:()=> {
                    return{
                        id: -1,
                        username: ''
                    }
                }
            }
        },
        data() {
            return {
                skills: [{
                    name: 'Vue',
                    percentage: 70
                }, {
                    name: 'JS',
                    percentage: 18
                }, {
                    name: 'Css',
                    percentage: 50
                }],
            }
        }
    }
</script>

<style lang="scss" scoped>
    .box-center {
        margin: 0 auto;
        display: table;
    }

    .text-muted {
        color: #777;
    }

    .user-profile {
        .user-name {
            font-weight: bold;
        }

        .box-center {
            padding-top: 10px;
        }

        .user-role {
            padding-top: 10px;
            font-weight: 400;
            font-size: 14px;
        }

        .box-social {
            padding-top: 30px;

            .el-table {
                border-top: 1px solid #dfe6ec;
            }
        }

        .user-follow {
            padding-top: 20px;
        }
    }

    .user-bio {
        margin-top: 20px;
        color: #606266;

        span {
            padding-left: 4px;
        }

        .user-bio-section {
            font-size: 14px;
            padding: 15px 0;

            .user-bio-section-header {
                border-bottom: 1px solid #dfe6ec;
                padding-bottom: 10px;
                margin-bottom: 10px;
                font-weight: bold;
            }
        }
    }
</style>
